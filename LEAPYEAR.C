#include<stdio.h>
void main()
{
int year=2024,month=2,date=26;
if(year%4==0,year%400==0)
printf("the following year is leap year%d/%d/%d",date,month,year);
else if
printf("the following year is not a leap year%d/%d/%d",date,month,year);
getch();
}