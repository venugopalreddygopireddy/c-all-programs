#include<stdio.h>
void main()
{
int dep=1000,interest,total_amount=0;
interest=dep*0.2;
total_amount=dep+interest;
printf("the total amount in the bank after one year=%d",total_amount);
getch();
}